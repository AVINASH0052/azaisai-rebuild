export default function AdminPage() {
  return (
    <main className="px-6 py-16">
      <h1 className="text-2xl text-fg">Admin</h1>
      <p className="mt-2 text-fg-muted">
        Read-only ops metrics land with billing. Schema is already workspace-keyed.
      </p>
    </main>
  );
}
